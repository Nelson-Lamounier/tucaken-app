# Hero — Conveyor Backdrop + Static Depth Pipeline Panel (Implementation Plan)

> **For agentic workers:** REQUIRED SUB-SKILL: superpowers:subagent-driven-development. Steps use `- [ ]` checkboxes.

**Goal:** Keep the full-bleed `ConveyorBelt` backdrop and add a STATIC layered 3D-depth pipeline (grid bg / isometric SVG dash-flow / Framer-Motion floating glass cards) as a right-side panel beside static hero copy. No mouse interaction anywhere.

**Architecture:** `HeroSection` = `<ConveyorBelt/>` backdrop + two-column grid (left static copy, right `<PipelineScene/>`). `PipelineScene` is a static `perspective`+`preserve-3d` composer (NO rotate, NO pointer, NO MotionValues) stacking `BgLayer` (z −150), `CoreLayer` (z 0), `CardLayer` (z +120). It reads `useReducedMotion()` only to pass `reduce` down.

**Tech Stack:** React 19, TS, Tailwind v4, Motion for React, Vitest + happy-dom + Testing Library.

**Retained unchanged:** `ConveyorBelt.tsx`, its tests, conveyor keyframes. **Restored from git `a43fa66`:** `pipeline-svg.tsx`. **Rewritten:** `PipelineCards.tsx`. **New:** `PipelineScene.tsx`.

---

## Conventions

- Tests under `src/__tests__/`, first line `/** @vitest-environment happy-dom */` (vitest default env `node`).
- Alias `@/`→`src/`. Single file: `yarn vitest run <path>`.
- Router mock: `vi.mock('@tanstack/react-router', () => ({ useNavigate: () => navigateMock }))`.
- NO `Co-Authored-By` trailer.
- Project Motion rules (.claude/rules/motion-react.md): import from `motion/react`; `"use client"` when motion hooks/components used; independent transforms (`y`, not transform string); `willChange` only transform/opacity/clipPath/filter; never read a MotionValue in render.
- `bg-gradient-*` / arbitrary `[..]` Tailwind classes are existing repo convention — IDE may suggest canonical forms; do NOT change (lint 0 errors).

## File Structure

- Modify `src/styles.css` — re-add `@keyframes pipe-pulse`, `@keyframes node-glow`, base `.pipe-pulse-anim`/`.node-glow-anim`; extend reduced-motion kill-switch. Conveyor keyframes stay.
- Create `src/features/home/lib/pipeline-svg.tsx` — `BgLayer`, `CoreLayer` (restored verbatim).
- Create `src/features/home/lib/PipelineCards.tsx` — `CardLayer` glassmorphism + Framer-Motion `y` float.
- Create `src/features/home/lib/PipelineScene.tsx` — static 3-layer depth composer.
- Modify `src/features/home/sections/HeroSection.tsx` — two-column over ConveyorBelt.
- Tests: `pipeline-svg.test.tsx`, `PipelineCards.test.tsx`, `PipelineScene.test.tsx`, replace `HeroSection.test.tsx`.

---

## Task 1: Re-add pulse/glow keyframes

**Files:** Modify `src/styles.css`

- [ ] **Step 1:** Append at end of `src/styles.css`:

```css

@keyframes pipe-pulse {
  to { stroke-dashoffset: -64; }
}
@keyframes node-glow {
  0%, 100% { opacity: 0.35; filter: drop-shadow(0 0 0 transparent); }
  50% { opacity: 1; filter: drop-shadow(0 0 6px oklch(0.8 0.15 175)); }
}
.pipe-pulse-anim {
  animation: pipe-pulse 1.6s linear infinite;
}
.node-glow-anim {
  animation: node-glow 2s ease-in-out infinite;
}
```

- [ ] **Step 2:** Find the existing conveyor reduced-motion block
`@media (prefers-reduced-motion: reduce) { .belt-scroll-anim, .scan-sweep-anim, .item-state-anim { animation: none !important; } }`
and replace that single rule's selector list to also include the two new classes, i.e. it becomes:

```css
@media (prefers-reduced-motion: reduce) {
  .belt-scroll-anim, .scan-sweep-anim, .item-state-anim, .pipe-pulse-anim, .node-glow-anim { animation: none !important; }
}
```

(There must remain exactly ONE `@media (prefers-reduced-motion: reduce)` block for hero animations. Do not duplicate it. Do not touch the conveyor `@keyframes`.)

- [ ] **Step 3:** Sanity — `yarn vitest run src/__tests__/features/onboarding/ReviewStep.test.tsx` → PASS.

- [ ] **Step 4:** Commit
```bash
git add src/styles.css
git commit -m "feat(hero): re-add pulse/glow keyframes for depth pipeline"
```

---

## Task 2: Restore pipeline-svg.tsx (Bg + Core layers)

**Files:** Create `src/features/home/lib/pipeline-svg.tsx`, test `src/__tests__/features/home/pipeline-svg.test.tsx`

- [ ] **Step 1:** Create test `src/__tests__/features/home/pipeline-svg.test.tsx`:

```tsx
/** @vitest-environment happy-dom */
import { describe, it, expect } from 'vitest'
import { render } from '@testing-library/react'
import { BgLayer, CoreLayer } from '@/features/home/lib/pipeline-svg'

describe('pipeline-svg', () => {
  it('BgLayer renders a grid svg', () => {
    const { container } = render(<BgLayer reduce={false} />)
    expect(container.querySelector('svg')).toBeTruthy()
    expect(container.querySelector('[data-pipeline="grid"]')).toBeTruthy()
  })

  it('CoreLayer renders cables and nodes', () => {
    const { container } = render(<CoreLayer reduce={false} />)
    expect(container.querySelectorAll('[data-pipeline="cable"]').length).toBeGreaterThan(0)
    expect(container.querySelectorAll('[data-pipeline="node"]').length).toBeGreaterThan(0)
  })

  it('CoreLayer drops pulse and glow animation classes when reduce=true', () => {
    const { container } = render(<CoreLayer reduce />)
    expect(container.querySelector('.pipe-pulse-anim')).toBeNull()
    expect(container.querySelector('.node-glow-anim')).toBeNull()
  })
})
```

- [ ] **Step 2:** Run → FAIL (module not found): `yarn vitest run src/__tests__/features/home/pipeline-svg.test.tsx`

- [ ] **Step 3:** Create `src/features/home/lib/pipeline-svg.tsx` EXACTLY (restored from git `a43fa66`):

```tsx
// src/features/home/lib/pipeline-svg.tsx
// Pure SVG depth layers for the hero pipeline. No motion values here —
// the depth camera is static. reduce disables the pulse/glow.

const NODES = [
  { x: 60, y: 120 },
  { x: 200, y: 80 },
  { x: 200, y: 170 },
  { x: 340, y: 120 },
  { x: 470, y: 120 },
]

const CABLES = [
  'M60 120 C 130 120, 130 80, 200 80',
  'M60 120 C 130 120, 130 170, 200 170',
  'M200 80 C 270 80, 270 120, 340 120',
  'M200 170 C 270 170, 270 120, 340 120',
  'M340 120 L 470 120',
]

export function BgLayer({ reduce }: { reduce: boolean }) {
  void reduce
  return (
    <svg viewBox="0 0 520 240" className="h-full w-full" aria-hidden>
      <defs>
        <pattern id="pipe-grid" width="26" height="26" patternUnits="userSpaceOnUse">
          <path d="M26 0 H0 V26" fill="none" stroke="oklch(0.7 0.05 220 / 0.12)" strokeWidth="1" />
        </pattern>
        <radialGradient id="pipe-ambient" cx="50%" cy="45%" r="60%">
          <stop offset="0%" stopColor="oklch(0.7 0.18 175 / 0.22)" />
          <stop offset="100%" stopColor="transparent" />
        </radialGradient>
      </defs>
      <rect data-pipeline="grid" width="520" height="240" fill="url(#pipe-grid)" />
      <rect width="520" height="240" fill="url(#pipe-ambient)" />
    </svg>
  )
}

export function CoreLayer({ reduce }: { reduce: boolean }) {
  const pulseClass = reduce ? '' : 'pipe-pulse-anim'
  const glowClass = reduce ? '' : 'node-glow-anim'
  return (
    <svg viewBox="0 0 520 240" className="h-full w-full" aria-hidden>
      {CABLES.map((d, i) => (
        <g key={`c${i}`}>
          <path data-pipeline="cable" d={d} fill="none" stroke="oklch(0.5 0.04 220)" strokeWidth="2" />
          <path
            className={pulseClass}
            d={d}
            fill="none"
            stroke="oklch(0.85 0.16 175)"
            strokeWidth="2.5"
            strokeDasharray="14 50"
            strokeLinecap="round"
            style={{
              filter: 'drop-shadow(0 0 4px oklch(0.85 0.16 175))',
              willChange: 'filter',
              animationDelay: `${i * 0.4}s`,
            }}
          />
        </g>
      ))}
      {NODES.map((n, i) => (
        <rect
          key={`n${i}`}
          data-pipeline="node"
          className={glowClass}
          x={n.x - 11}
          y={n.y - 11}
          width="22"
          height="22"
          rx="4"
          transform={`rotate(45 ${n.x} ${n.y})`}
          fill="oklch(0.22 0.03 220)"
          stroke="oklch(0.7 0.14 175)"
          strokeWidth="1.5"
          style={{ animationDelay: `${i * 0.5}s` }}
        />
      ))}
    </svg>
  )
}
```

- [ ] **Step 4:** Run → PASS (3 tests): `yarn vitest run src/__tests__/features/home/pipeline-svg.test.tsx`

- [ ] **Step 5:** Commit
```bash
git add src/features/home/lib/pipeline-svg.tsx src/__tests__/features/home/pipeline-svg.test.tsx
git commit -m "feat(hero): restore Bg/Core SVG depth layers"
```

---

## Task 3: PipelineCards.tsx — glassmorphism + Framer-Motion float

**Files:** Create `src/features/home/lib/PipelineCards.tsx`, test `src/__tests__/features/home/PipelineCards.test.tsx`

- [ ] **Step 1:** Create test `src/__tests__/features/home/PipelineCards.test.tsx`:

```tsx
/** @vitest-environment happy-dom */
import { describe, it, expect, vi } from 'vitest'
import { render, screen } from '@testing-library/react'

describe('CardLayer', () => {
  it('renders the three glass status cards', async () => {
    vi.resetModules()
    const { CardLayer } = await import('@/features/home/lib/PipelineCards')
    render(<CardLayer reduce={false} />)
    expect(screen.getByText('Lead Discovered')).toBeTruthy()
    expect(screen.getByText('Call Initiated')).toBeTruthy()
    expect(screen.getByText('Resume Grounded')).toBeTruthy()
  })

  it('renders without crashing under reduced motion', async () => {
    vi.resetModules()
    const { CardLayer } = await import('@/features/home/lib/PipelineCards')
    const { container } = render(<CardLayer reduce />)
    expect(container.querySelectorAll('[data-card="float"]').length).toBe(3)
  })
})
```

- [ ] **Step 2:** Run → FAIL (module not found): `yarn vitest run src/__tests__/features/home/PipelineCards.test.tsx`

- [ ] **Step 3:** Create `src/features/home/lib/PipelineCards.tsx`:

```tsx
"use client"
// src/features/home/lib/PipelineCards.tsx
// Foreground glassmorphism status cards. Independent continuous y-float via
// Framer Motion so they stay alive when the cursor is still. reduce collapses
// the float to a static position.

import { motion } from 'motion/react'

const CARDS = [
  { label: 'Lead Discovered', dot: 'bg-teal-400', pos: 'left-0 top-4', dur: 5, delay: 0 },
  { label: 'Call Initiated', dot: 'bg-cyan-400', pos: 'right-2 top-16', dur: 6, delay: 0.5 },
  { label: 'Resume Grounded', dot: 'bg-emerald-400', pos: 'left-10 bottom-6', dur: 7, delay: 1 },
] as const

export function CardLayer({ reduce }: { reduce: boolean }) {
  return (
    <div className="absolute inset-0">
      {CARDS.map((c) => (
        <motion.div
          key={c.label}
          data-card="float"
          className={`absolute ${c.pos} rounded-xl border border-white/15 bg-white/5 px-3 py-2 shadow-xl shadow-black/30 backdrop-blur-xl`}
          style={{ willChange: 'transform' }}
          animate={reduce ? { y: 0 } : { y: [0, -10, 0] }}
          transition={
            reduce
              ? { duration: 0 }
              : { duration: c.dur, delay: c.delay, repeat: Infinity, ease: 'easeInOut' }
          }
        >
          <span className={`mr-2 inline-block h-1.5 w-1.5 rounded-full ${c.dot} shadow-[0_0_8px_currentColor]`} />
          <span className="font-mono text-[11px] text-zinc-100">{c.label}</span>
        </motion.div>
      ))}
    </div>
  )
}
```

- [ ] **Step 4:** Run → PASS (2 tests): `yarn vitest run src/__tests__/features/home/PipelineCards.test.tsx`

- [ ] **Step 5:** Commit
```bash
git add src/features/home/lib/PipelineCards.tsx src/__tests__/features/home/PipelineCards.test.tsx
git commit -m "feat(hero): glassmorphism CardLayer with Framer-Motion float"
```

---

## Task 4: PipelineScene.tsx — static 3-layer depth composer

**Files:** Create `src/features/home/lib/PipelineScene.tsx`, test `src/__tests__/features/home/PipelineScene.test.tsx`

- [ ] **Step 1:** Create test `src/__tests__/features/home/PipelineScene.test.tsx`:

```tsx
/** @vitest-environment happy-dom */
import { describe, it, expect } from 'vitest'
import { render } from '@testing-library/react'
import { PipelineScene } from '@/features/home/lib/PipelineScene'

describe('PipelineScene', () => {
  it('renders the three depth layers', () => {
    const { container } = render(<PipelineScene />)
    expect(container.querySelector('[data-scene="bg"]')).toBeTruthy()
    expect(container.querySelector('[data-scene="core"]')).toBeTruthy()
    expect(container.querySelector('[data-scene="cards"]')).toBeTruthy()
  })

  it('has a static preserve-3d stage with no rotation transform', () => {
    const { container } = render(<PipelineScene />)
    const stage = container.querySelector('[data-scene="stage"]') as HTMLElement
    expect(stage).toBeTruthy()
    expect(stage.style.transformStyle).toBe('preserve-3d')
    // static camera: no rotateX/rotateY in the stage transform
    expect(stage.style.transform || '').not.toMatch(/rotate/i)
  })
})
```

- [ ] **Step 2:** Run → FAIL (module not found): `yarn vitest run src/__tests__/features/home/PipelineScene.test.tsx`

- [ ] **Step 3:** Create `src/features/home/lib/PipelineScene.tsx`:

```tsx
"use client"
// src/features/home/lib/PipelineScene.tsx
// Static layered 3D-depth pipeline panel. Fixed camera — NO pointer, NO
// rotate, NO MotionValues. The only hook is useReducedMotion(), used purely
// to pass `reduce` to children (not an interaction). Depth comes from fixed
// per-layer translateZ inside a perspective + preserve-3d container.

import { useReducedMotion } from 'motion/react'
import { BgLayer, CoreLayer } from './pipeline-svg'
import { CardLayer } from './PipelineCards'

export function PipelineScene() {
  const reduce = useReducedMotion() ?? false
  return (
    <div className="relative h-[460px] w-full" style={{ perspective: '1200px' }}>
      <div
        data-scene="stage"
        className="absolute inset-0"
        style={{ transformStyle: 'preserve-3d' }}
      >
        <div
          data-scene="bg"
          className="pointer-events-none absolute inset-0"
          style={{ transform: 'translateZ(-150px) scale(1.18)' }}
        >
          <BgLayer reduce={reduce} />
        </div>
        <div
          data-scene="core"
          className="pointer-events-none absolute inset-0"
          style={{ transform: 'translateZ(0px)' }}
        >
          <CoreLayer reduce={reduce} />
        </div>
        <div
          data-scene="cards"
          className="absolute inset-0"
          style={{ transform: 'translateZ(120px)' }}
        >
          <CardLayer reduce={reduce} />
        </div>
      </div>
    </div>
  )
}
```

- [ ] **Step 4:** Run → PASS (2 tests): `yarn vitest run src/__tests__/features/home/PipelineScene.test.tsx`

- [ ] **Step 5:** Commit
```bash
git add src/features/home/lib/PipelineScene.tsx src/__tests__/features/home/PipelineScene.test.tsx
git commit -m "feat(hero): add static PipelineScene depth composer"
```

---

## Task 5: HeroSection — two-column over ConveyorBelt

**Files:** Modify `src/features/home/sections/HeroSection.tsx`, replace test `src/__tests__/features/home/HeroSection.test.tsx`

- [ ] **Step 1:** Replace entire `src/__tests__/features/home/HeroSection.test.tsx`:

```tsx
/** @vitest-environment happy-dom */
import { describe, it, expect, vi, beforeEach } from 'vitest'
import { render, screen } from '@testing-library/react'

const navigateMock = vi.fn()
vi.mock('@tanstack/react-router', () => ({
  useNavigate: () => navigateMock,
}))

import { HeroSection } from '@/features/home/sections/HeroSection'
import { hero } from '@/features/home/content'

describe('HeroSection', () => {
  beforeEach(() => navigateMock.mockReset())

  it('renders eyebrow, headline, founder note and both CTAs', () => {
    render(<HeroSection />)
    expect(screen.getByText(hero.eyebrow)).toBeTruthy()
    expect(screen.getByText(/already proves/i)).toBeTruthy()
    expect(screen.getByText(hero.founderNote)).toBeTruthy()
    expect(screen.getByRole('button', { name: new RegExp(hero.primaryCta, 'i') })).toBeTruthy()
    expect(screen.getByRole('button', { name: new RegExp(hero.secondaryCta, 'i') })).toBeTruthy()
  })

  it('both CTAs navigate to /sign-in', async () => {
    const { default: userEvent } = await import('@testing-library/user-event')
    render(<HeroSection />)
    await userEvent.click(screen.getByRole('button', { name: new RegExp(hero.primaryCta, 'i') }))
    await userEvent.click(screen.getByRole('button', { name: new RegExp(hero.secondaryCta, 'i') }))
    expect(navigateMock).toHaveBeenCalledTimes(2)
    expect(navigateMock).toHaveBeenCalledWith({ to: '/sign-in' })
  })

  it('renders conveyor backdrop AND the static depth pipeline panel', () => {
    const { container } = render(<HeroSection />)
    expect(container.querySelector('[data-belt="root"]')).toBeTruthy()
    expect(container.querySelector('[data-scene="stage"]')).toBeTruthy()
    expect(container.querySelector('[data-layer="copy"]')).toBeNull()
  })
})
```

- [ ] **Step 2:** Run → FAIL (no `[data-scene="stage"]` yet): `yarn vitest run src/__tests__/features/home/HeroSection.test.tsx`

- [ ] **Step 3:** Replace entire `src/features/home/sections/HeroSection.tsx`:

```tsx
"use client"
// src/features/home/sections/HeroSection.tsx
import { motion } from 'motion/react'
import { useNavigate } from '@tanstack/react-router'
import { MagneticButton } from '../lib/MagneticButton'
import { ConveyorBelt } from '../lib/ConveyorBelt'
import { PipelineScene } from '../lib/PipelineScene'
import { hero } from '../content'

export function HeroSection() {
  const navigate = useNavigate()

  return (
    <div className="relative overflow-hidden border-b border-white/5 bg-zinc-950">
      <ConveyorBelt />

      <div className="relative mx-auto grid min-h-[560px] max-w-6xl items-center gap-12 px-6 py-24 md:grid-cols-12 md:px-12 md:py-32">
        <div className="md:col-span-6">
          <motion.div
            initial={{ opacity: 0 }}
            animate={{ opacity: 1 }}
            transition={{ duration: 0.5 }}
            className="inline-flex w-fit items-center gap-2 rounded-full border border-teal-500/30 bg-teal-500/10 px-3 py-1 backdrop-blur-md"
          >
            <span className="h-1.5 w-1.5 rounded-full bg-teal-400 shadow-[0_0_8px_currentColor]" />
            <span className="font-mono text-[10px] uppercase tracking-widest text-teal-200">{hero.eyebrow}</span>
          </motion.div>

          <h1 className="mt-7 text-balance text-4xl font-semibold leading-[1.02] tracking-tight md:text-6xl">
            <span className="block text-white">Your GitHub already proves</span>
            <span className="block text-white">you can do the job.</span>
            <span className="block bg-gradient-to-r from-teal-300 via-emerald-300 to-cyan-300 bg-clip-text text-transparent">
              Now your resume can.
            </span>
          </h1>

          <div className="mt-8 flex flex-wrap items-center gap-3">
            <MagneticButton primary onClick={() => navigate({ to: '/sign-in' })}>
              ⌥ {hero.primaryCta}
            </MagneticButton>
            <MagneticButton onClick={() => navigate({ to: '/sign-in' })}>
              {hero.secondaryCta}
            </MagneticButton>
          </div>
          <div className="mt-5 font-mono text-[11px] text-zinc-500">{hero.founderNote}</div>
        </div>

        <div className="hidden md:col-span-6 md:block">
          <PipelineScene />
        </div>
      </div>
    </div>
  )
}
```

- [ ] **Step 4:** Run → PASS (3 tests): `yarn vitest run src/__tests__/features/home/HeroSection.test.tsx`

- [ ] **Step 5:** Regression — `yarn test && yarn lint`. Expect full suite GREEN; `yarn lint` 0 errors (pre-existing repo-wide `no-console` warnings OK; no NEW errors in touched files).

- [ ] **Step 6:** Commit
```bash
git add src/features/home/sections/HeroSection.tsx src/__tests__/features/home/HeroSection.test.tsx
git commit -m "feat(hero): two-column hero — copy + static PipelineScene over ConveyorBelt"
```

---

## Task 6: Performance gate

**Files:** none (verification only)

- [ ] **Step 1:** motion-audit the changed files (`pipeline-svg.tsx`, `PipelineCards.tsx`, `PipelineScene.tsx`, `HeroSection.tsx`, `styles.css`). Expected tiers: card `y` float = S (Motion transform/opacity); SVG dash pulse = C (stroke-dashoffset paint) but explicitly user-requested and reduced-motion gated — acceptable; node-glow = S (opacity+filter); no layout/thrash. Confirm reduced-motion kill-switch covers `.pipe-pulse-anim`/`.node-glow-anim`; confirm `CardLayer` collapses `y` to 0 under `reduce`. Fix only if something ranks D/F or loops unguarded.

- [ ] **Step 2:** Optional smoke — `yarn dev`: conveyor scrolls behind; right panel shows static-perspective grid bg + glowing SVG dash-flow + 3 glass cards floating; headline/CTAs perfectly static; OS reduce-motion freezes pulse + card float.

---

## Self-Review (plan author)

- **Spec (Revision 2) coverage:** ConveyorBelt retained (Task 5 imports it, untouched) ✓; bg grid layer z −150 (Task 2 BgLayer + Task 4 translateZ) ✓; isometric SVG midground z 0 with strokeDasharray/strokeDashoffset infinite linear pulse (Task 2 CoreLayer + Task 1 `pipe-pulse` keyframe) ✓; glass cards z +120 with Framer-Motion `y:[0,-10,0]` float (Task 3 + Task 4) ✓; labels "Lead Discovered"/"Call Initiated"/"Resume Grounded" ✓; static camera, no pointer/rotate/MotionValues (Task 4 — test asserts no `rotate` in stage transform) ✓; right-side panel, left static copy (Task 5 two-column grid) ✓; reduced motion: CSS kill-switch incl. pulse/glow (Task 1) + card `y`→0 (Task 3) ✓; perf gate (Task 6) ✓.
- **Placeholder scan:** none — every step has concrete code/commands.
- **Type consistency:** `BgLayer`/`CoreLayer`/`CardLayer` all `{ reduce: boolean }`; `PipelineScene` zero-prop, supplies `reduce` via `useReducedMotion() ?? false`; `HeroSection` imports `ConveyorBelt` + `PipelineScene`; tests assert `data-pipeline`/`data-card`/`data-scene`/`data-belt` attrs the components emit and ABSENCE of legacy `data-layer="copy"`. `hero.eyebrow/primaryCta/secondaryCta/founderNote` confirmed in `content.ts`.
- **Note:** `card-float` keyframe intentionally NOT re-added (cards use Framer Motion now). Only ONE `@media (prefers-reduced-motion: reduce)` hero block must exist after Task 1 (extended selector, not duplicated).
