# Repository Folder Structure — Snapshot

_Generated: 2026-05-04. Branch: `fix/isNew-user-provisioning`._

This is a frozen snapshot of the `src/` tree used as the basis for `review-2026-05-04.md`. Regenerate with:

```bash
tree -I 'node_modules|dist|.yarn|.git|.tanstack' -L 4 --dirsfirst src
```

## Top-level layout

```text
src
├── __tests__/             # Centralised tests (server-only at the moment)
├── app/                   # TanStack Router flat-file routes
├── components/            # Cross-feature UI primitives + layouts
│   ├── layouts/           # AppLayout, DashboardPage
│   ├── resume/            # Resume document renderers (cross-feature)
│   └── ui/                # 25 generic UI atoms (Button, DropDown, Tabs…)
├── contexts/              # React context providers (ThemeContext only)
├── features/              # Feature-sliced modules (see below)
├── hooks/                 # Cross-feature hooks (admin queries, theme, pdf)
├── images/                # Static imports
├── lib/                   # Shared, framework-agnostic code
│   ├── api/               # query-keys factory
│   ├── auth/              # tanstack-auth helpers
│   ├── observability/     # Faro, logger, metrics, telemetry
│   ├── resumes/           # Resume data + DOM builders
│   ├── stores/            # Zustand stores (toast, pipeline, applications)
│   └── types/             # DTO-style shared types
├── server/                # `createServerFn` modules (one file per domain)
├── types/                 # Global TS types
├── router.tsx             # TanStack Router config
├── routeTree.gen.ts       # Auto-generated by router-plugin
└── styles.css             # Tailwind v4 entry
```

## Routes (`src/app/`)

Flat-file convention. Dots = path segments, `_dashboard.*` = layout group.

```text
__root.tsx                      # App shell
_dashboard.tsx                  # Dashboard layout group
_dashboard.applications.list.tsx
_dashboard.applications.$slug.tsx
_dashboard.applications.new.tsx
_dashboard.applications.index.tsx
_dashboard.applications.interview-prep.tsx
_dashboard.articles.tsx
_dashboard.ai-agent.tsx
_dashboard.calendar.tsx
_dashboard.comments.tsx
_dashboard.overview.tsx
_dashboard.reports.tsx
_dashboard.resumes.tsx
_dashboard.resumes.new.tsx
_dashboard.resumes.edit.$id.tsx
_dashboard.settings.github.tsx
_dashboard.test.tsx
_dashboard.$.tsx                # Catch-all
index.tsx
login.tsx
sign-in.tsx
sign-in.callback.tsx
onboarding.tsx
```

**Missing**: no `_dashboard.settings.tsx` parent, no `_dashboard.account.*` or `_dashboard.billing.*` routes — yet `src/features/account/` exists with full BillingPage + SettingsPage.

## Features (`src/features/`)

Per-feature shape (where present): `components/`, `hooks/`, sometimes `lib/`, occasionally a top-level `*.ts` for queries/types/validation.

| Feature        | components | hooks | other                          | Mounted by route?              |
|----------------|-----------:|------:|--------------------------------|--------------------------------|
| account        | 2          | —     | billing/, settings/, types.ts  | **NO** (orphan)                |
| ai-agent       | 8          | 3     | —                              | yes                            |
| applications   | 13         | 3     | —                              | yes                            |
| articles       | 5          | —     | —                              | yes                            |
| auth           | 11         | —     | validation.ts                  | yes                            |
| calendars      | 4          | —     | —                              | yes                            |
| comments       | 5          | —     | —                              | yes                            |
| github         | 5          | 4     | —                              | yes                            |
| home           | —          | —     | sections/, lib/, content.ts    | yes (`/`)                      |
| onboarding     | 4+         | —     | steps/, onboarding/            | yes                            |
| overview       | 1          | —     | —                              | yes                            |
| reports        | 2          | —     | queries.ts                     | yes                            |
| resumes        | 4          | —     | —                              | yes                            |

## Server (`src/server/`)

Domain-per-file `createServerFn` modules. No subfolders.

```text
_api-client.ts        applications.ts     articles.ts
auth.ts               auth-callback.ts    auth-guard.ts
comments.ts           draft-publish.ts    finops.ts
github.ts             me.ts               patches.ts
pipelines.ts          prompt-feedback.ts  resume-imports.ts
resumes.ts            security-headers.ts session.ts
upload.ts
```

## Tests (`src/__tests__/`)

```text
__tests__/server/
├── applications.test.ts
├── articles.test.ts
├── auth.test.ts
└── github.test.ts
```

Plus one co-located: `src/app/_dashboard.test.tsx`.

**Coverage gap**: 4 server domains tested out of 19. Zero feature/component/hook tests.

## Path aliases (`tsconfig.json`)

```json
"paths": {
  "#/*": ["./src/*"],
  "@/*": ["./src/*"]
}
```

`package.json#imports` adds `"#/*": "./src/*"` for runtime. Both aliases point to the same place — pick one.

## Counts

- TS/TSX files in `src/`: **226**
- Routes: **24**
- Features: **13** (12 wired, 1 orphan)
- Shared UI atoms: **25**
- Server domain files: **19**
- Test files: **5** (4 server + 1 route)
- Imports using `@/`: **158**
- Imports using `../`: **150** (many `../../../`)
