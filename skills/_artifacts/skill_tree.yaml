# skills/_artifacts/skill_tree.yaml
# Generated by skill-tree-generator
# Workflow: A (Generate)
library:
  name: 'tucaken-app'
  version: 'initial-development'
  repository: 'private'
  description: >
    SaaS application built on TanStack Start (SSR React), TanStack Router,
    TanStack Query v5, Tailwind CSS v4, and Zustand. BFF layer via
    createServerFn delegates to a live Kubernetes-hosted admin-api.
generated_from:
  domain_map: 'skills/_artifacts/domain_map.yaml'
  skill_spec: 'skills/_artifacts/skill_spec.md'
generated_at: '2026-04-30'

# Structure: FLAT — 14 task-focused skills map 1:1 to developer intents.
# No core/framework split (single-stack React app, not a published library).
# Checklist body used for: security-review, pre-deploy-checklist,
# ship-feature, getting-started.
# Reference file added for: component-reuse-audit (20+ component catalogue).

skills:
  # ── Core skills (standard body: Setup → Patterns → Mistakes) ─────────────

  - name: 'Add Route'
    slug: add-route
    type: core
    domain: routing
    path: 'skills/add-route/SKILL.md'
    description: >
      Add a TanStack Router file-based route to Tucaken. Covers _dashboard
      filename prefix for auth inheritance, createFileRoute, Zod validateSearch,
      DashboardPage wrapper, and AppLayout navigation wiring.
    requires: []
    sources:
      - 'tucaken-app:src/app/_dashboard.tsx'
      - 'tucaken-app:src/app/_dashboard.settings.github.tsx'
      - 'tucaken-app:src/components/layouts/AppLayout.tsx'
      - 'tucaken-app:src/components/layouts/DashboardPage.tsx'
    subsystems: []
    references: []

  - name: 'Router Architecture'
    slug: router-architecture
    type: lifecycle
    domain: routing
    path: 'skills/router-architecture/SKILL.md'
    description: >
      Migrate Tucaken from flat _dashboard.*.tsx files to TanStack Router
      nested route groups. Covers directory-based nesting, layout route files,
      URL preservation during migration, and preventing further flat-file sprawl.
    requires:
      - add-route
    sources:
      - 'tucaken-app:src/app/'
      - 'tucaken-app:vite.config.ts'
    subsystems: []
    references: []

  - name: 'Add Query'
    slug: add-query
    type: core
    domain: data-fetching
    path: 'skills/add-query/SKILL.md'
    description: >
      Create a TanStack Query hook for Tucaken using the adminKeys factory,
      conditional polling for pending/syncing states, queryClient invalidation
      after mutations, and useToastStore for feedback.
    requires: []
    sources:
      - 'tucaken-app:src/lib/api/query-keys.ts'
      - 'tucaken-app:src/features/github/hooks/use-github-connected-repos.ts'
      - 'tucaken-app:src/features/github/hooks/use-github-ingestion.ts'
      - 'tucaken-app:src/features/github/hooks/use-github-installation.ts'
    subsystems: []
    references: []

  - name: 'Add Server Function'
    slug: add-server-function
    type: core
    domain: server-functions
    path: 'skills/add-server-function/SKILL.md'
    description: >
      Create a TanStack Start createServerFn BFF handler in Tucaken. Covers
      method selection (GET vs POST), requireAuth guard, apiFetch helper,
      Zod inputValidator, ADMIN_API_URL env var, and Vitest mock pattern.
    requires: []
    sources:
      - 'tucaken-app:src/server/github.ts'
      - 'tucaken-app:src/server/applications.ts'
      - 'tucaken-app:src/server/auth-guard.ts'
      - 'tucaken-app:src/server/security-headers.ts'
      - 'tucaken-app:src/__tests__/server/github.test.ts'
    subsystems: []
    references:
      - 'skills/add-server-function/references/vitest-mock-pattern.md'

  - name: 'Add Feature Domain'
    slug: add-feature-domain
    type: core
    domain: feature-modules
    path: 'skills/add-feature-domain/SKILL.md'
    description: >
      Scaffold a new Tucaken feature module under src/features/<domain>/.
      Covers canonical directory structure, type file placement, query key
      extension, server function separation, TanStack Form for user-facing
      forms, and reuse-first discipline before creating new code.
    requires:
      - add-server-function
      - add-query
      - component-reuse-audit
    sources:
      - 'tucaken-app:src/features/github/'
      - 'tucaken-app:src/lib/types/github.types.ts'
      - 'tucaken-app:src/lib/api/query-keys.ts'
    subsystems: []
    references: []

  - name: 'Component Reuse Audit'
    slug: component-reuse-audit
    type: core
    domain: ui-components
    path: 'skills/component-reuse-audit/SKILL.md'
    description: >
      Before creating any UI in Tucaken, audit src/components/ui/ and
      src/components/layouts/ for matching patterns. Covers the full
      component catalogue, Button variants, DashboardPage, LinkCard,
      GridListActions, Motion for React import rules, and compose-vs-create
      decision criteria.
    requires: []
    sources:
      - 'tucaken-app:src/components/ui/Button.tsx'
      - 'tucaken-app:src/components/layouts/DashboardPage.tsx'
      - 'tucaken-app:src/components/ui/LinkCards.tsx'
      - 'tucaken-app:src/components/ui/GridListActions.tsx'
      - 'tucaken-app:.claude/rules/motion-react.md'
    subsystems: []
    references:
      - 'skills/component-reuse-audit/references/component-catalogue.md'

  - name: 'Code Optimisation'
    slug: code-optimisation
    type: core
    domain: performance
    path: 'skills/code-optimisation/SKILL.md'
    description: >
      Fix render inefficiencies, SSR hydration issues, and bundle bloat in
      Tucaken. Covers window/localStorage SSR guards, React 19 memoisation,
      lazy-loaded devtools pattern, Tailwind v4 SSR build constraints, and
      Motion for React willChange + independent transforms.
    requires: []
    sources:
      - 'tucaken-app:src/app/__root.tsx'
      - 'tucaken-app:vite.config.ts'
      - 'tucaken-app:.claude/rules/motion-react.md'
    subsystems: []
    references: []

  - name: 'Verify Endpoint Connectivity'
    slug: verify-endpoint-connectivity
    type: core
    domain: deployment
    path: 'skills/verify-endpoint-connectivity/SKILL.md'
    description: >
      Confirm a Tucaken server function reaches the correct admin-api pod.
      Covers ADMIN_API_URL resolution, Authorization Bearer forwarding,
      apiFetch error surface, Vitest fetch mock for URL/header assertions,
      and K8s pod log access. Admin-api runs live in the cluster (cdk-monitoring repo).
    requires:
      - add-server-function
    sources:
      - 'tucaken-app:src/server/applications.ts'
      - 'tucaken-app:src/__tests__/server/github.test.ts'
    subsystems: []
    references: []

  - name: 'Maintainability Review'
    slug: maintainability-review
    type: core
    domain: maintainability
    path: 'skills/maintainability-review/SKILL.md'
    description: >
      Assess a Tucaken module for structure drift, duplication, and
      point-in-time fixes. Covers canonical file placement rules,
      Zustand-vs-TanStack Query scope, naming conventions, and the
      agent-specific failure pattern of solving the immediate task
      without considering codebase lifecycle.
    requires: []
    sources:
      - 'tucaken-app:src/lib/stores/'
      - 'tucaken-app:src/features/github/'
      - 'tucaken-app:src/server/'
    subsystems: []
    references: []

  - name: 'Restructure Module'
    slug: restructure-module
    type: core
    domain: maintainability
    path: 'skills/restructure-module/SKILL.md'
    description: >
      Move or rename Tucaken files and directories without breaking @/ imports,
      TanStack Router route derivation, AppLayout navigation, or CI. Covers
      the post-move verification sequence: tsc --noEmit → vitest run → nav update.
    requires:
      - maintainability-review
    sources:
      - 'tucaken-app:vite.config.ts'
      - 'tucaken-app:tsconfig.json'
      - 'tucaken-app:src/components/layouts/AppLayout.tsx'
    subsystems: []
    references: []

  # ── Lifecycle / checklist skills (audit body template) ────────────────────

  - name: 'Getting Started'
    slug: getting-started
    type: lifecycle
    domain: feature-modules
    path: 'skills/getting-started/SKILL.md'
    description: >
      Onboard to Tucaken: Yarn 4 setup, @/ path alias, local dev (admin-api
      runs live in K8s cluster via cdk-monitoring repo), BFF boundary
      (tucaken-app owns UI + server fns; admin-api owns DB + K8s jobs),
      TanStack devtools, and the canonical src/ directory map.
    requires: []
    sources:
      - 'tucaken-app:package.json'
      - 'tucaken-app:vite.config.ts'
      - 'tucaken-app:src/app/__root.tsx'
    subsystems: []
    references: []

  - name: 'Ship Feature'
    slug: ship-feature
    type: lifecycle
    domain: feature-modules
    path: 'skills/ship-feature/SKILL.md'
    description: >
      End-to-end checklist for shipping a Tucaken feature: component reuse
      audit → server fn tests → typecheck → lint → vitest → smoke test →
      conventional commit → CI green → merge to main → ArgoCD deploy.
    requires:
      - component-reuse-audit
      - add-server-function
      - security-review
    sources:
      - 'tucaken-app:.github/workflows/ci.yml'
      - 'tucaken-app:.github/workflows/deploy.yml'
    subsystems: []
    references: []

  - name: 'Security Review'
    slug: security-review
    type: security
    domain: security
    path: 'skills/security-review/SKILL.md'
    description: >
      Pre-merge security audit for Tucaken. Checks requireAuth on every
      server function, securityHeadersMiddleware scope, Zod inputValidator
      presence, encodeURIComponent for path params, VITE_ prefix isolation,
      and CSP connect-src coverage for new third-party services.
    requires:
      - add-server-function
    sources:
      - 'tucaken-app:src/server/auth-guard.ts'
      - 'tucaken-app:src/server/security-headers.ts'
      - 'tucaken-app:src/server/github.ts'
    subsystems: []
    references: []

  - name: 'Pre-Deploy Checklist'
    slug: pre-deploy-checklist
    type: lifecycle
    domain: deployment
    path: 'skills/pre-deploy-checklist/SKILL.md'
    description: >
      Pre-deploy gate for Tucaken: CI jobs (lint, typecheck, test, build,
      docker-build) green, ADMIN_API_URL and VITE_* env vars wired, ArgoCD
      sync confirmed, Argo Rollouts promoted, CloudFront /admin/* invalidated.
    requires:
      - security-review
      - verify-endpoint-connectivity
    sources:
      - 'tucaken-app:.github/workflows/ci.yml'
      - 'tucaken-app:.github/workflows/deploy.yml'
    subsystems: []
    references: []
