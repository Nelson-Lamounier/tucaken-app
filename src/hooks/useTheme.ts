/**
 * Re-exports `useTheme` from the shared ThemeContext.
 * Import from here or directly from `@/contexts/ThemeContext` — both resolve to the same hook.
 */
export { useTheme } from '../contexts/ThemeContext'
